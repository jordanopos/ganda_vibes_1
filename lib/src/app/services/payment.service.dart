import 'package:ganda_vibes/src/utils/models/mobilemoney.request.model.dart';
import 'package:ganda_vibes/src/utils/models/mobilemoneyresponse.model.dart';
import 'package:logger/logger.dart';

class PaymentService {
  Future<FlutterwaveResponseModel?> paywithmobilemoney(
      FlutterwaveRequestModel model) async {
    var logger = Logger();

    logger.d("the flutter google controlelr is not working");

    return null;
  }
}
